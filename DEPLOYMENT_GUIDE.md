# 个人作品集网站部署文档

## 项目概述

这是一个睡眠工程师的个人作品集网站，包含以下核心功能：
- 个人介绍和服务展示
- 留言板功能
- 睡眠数据记录功能
- 响应式设计，支持手机和电脑

本文档将指导您如何配置云端连接，使留言板和睡眠数据能够发送到后台，并部署到Vercel或其他平台。

## 环境准备

### 1. 安装Node.js

确保您的系统已经安装了Node.js（版本14.0.0或更高）：

```bash
# 检查Node.js版本
node -v

# 检查npm版本
npm -v
```

如果没有安装Node.js，请从[官网](https://nodejs.org/)下载并安装。

### 2. 安装依赖

在项目根目录下运行以下命令安装依赖：

```bash
npm install
```

## CloudBase配置

### 1. 创建CloudBase环境

1. 访问[腾讯云CloudBase控制台](https://console.cloud.tencent.com/tcb)
2. 登录您的腾讯云账号
3. 点击"创建环境"
4. 填写环境名称（例如：`sleep-engineer`）
5. 选择地域（建议选择`ap-shanghai`）
6. 选择免费版或付费版
7. 点击"立即开通"

### 2. 获取环境信息

环境创建完成后，您将获得以下重要信息：
- **环境ID**：用于标识您的CloudBase环境
- **地域**：环境所在的地域

### 3. 创建数据库集合

在CloudBase控制台中：

1. 选择您创建的环境
2. 点击左侧菜单中的"数据库"
3. 点击"创建集合"
4. 创建以下集合：
   - `guestbook`：用于存储留言数据
   - `sleep_data`：用于存储睡眠数据

### 4. 配置数据库权限

为了确保数据安全，建议配置适当的数据库权限：

1. 点击集合名称右侧的"权限配置"
2. 选择"自定义安全规则"
3. 配置以下规则：

```json
{
  "read": true,
  "write": "auth != null",
  "index": true
}
```

或者，为了简化开发，您可以暂时将权限设置为"所有用户可读可写"，但在生产环境中请务必配置更严格的权限。

## 本地开发配置

### 1. 创建环境变量文件

在项目根目录下创建一个`.env`文件，用于存储环境变量：

```bash
# 创建.env文件
cp .env.example .env
```

如果没有`.env.example`文件，可以手动创建：

```bash
touch .env
```

### 2. 配置环境变量

在`.env`文件中添加以下内容：

```env
# CloudBase配置
CLOUDBASE_ENV_ID=您的CloudBase环境ID
CLOUDBASE_REGION=ap-shanghai

# 网站配置
SITE_NAME=Deep Sleeper | 睡眠工程师
SITE_DESCRIPTION=专业睡眠工程师，提供深度睡眠优化服务

# API配置
API_PREFIX=/api

# CORS配置
CORS_ORIGIN=*

# 端口配置
PORT=3000
```

### 3. 启动本地服务器

运行以下命令启动本地开发服务器：

```bash
npm run dev
```

服务器将在`http://localhost:3000`上运行。

### 4. 测试功能

在浏览器中访问`http://localhost:3000`，测试以下功能：

1. **留言板功能**：
   - 填写留言表单
   - 点击"提交留言"
   - 检查留言是否成功提交
   - 查看留言列表

2. **睡眠数据记录功能**：
   - 调整睡眠时长和质量滑块
   - 填写备注
   - 点击"记录睡眠"
   - 检查睡眠数据是否成功记录

## 部署到Vercel

### 1. 创建Vercel账号

如果您还没有Vercel账号，请访问[Vercel官网](https://vercel.com/)注册一个账号。

### 2. 安装Vercel CLI

```bash
npm install -g vercel
```

### 3. 登录Vercel

```bash
vercel login
```

### 4. 部署项目

在项目根目录下运行以下命令：

```bash
vercel
```

按照提示完成部署流程：

1. 选择您的Vercel团队或个人账号
2. 确认项目名称
3. 确认项目根目录
4. 选择框架（选择"Other"）
5. 配置环境变量（与本地`.env`文件内容相同）

### 5. 查看部署结果

部署完成后，您将获得一个Vercel提供的域名，例如：`https://your-project.vercel.app`

访问该域名，测试网站功能是否正常。

### 6. 配置自定义域名（可选）

如果您有自己的域名，可以在Vercel控制台中配置自定义域名：

1. 访问Vercel控制台
2. 选择您的项目
3. 点击"Settings" -> "Domains"
4. 输入您的域名
5. 按照提示配置DNS记录

## 微信小程序部署（可选）

### 1. 准备工作

- 注册微信小程序账号：访问[微信公众平台](https://mp.weixin.qq.com/)
- 下载并安装[微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)

### 2. 配置小程序

1. 在微信公众平台中创建小程序
2. 记录小程序的AppID
3. 在`miniprogram`目录下修改`project.config.json`文件，将`appid`字段替换为您的小程序AppID

### 3. 导入项目

1. 打开微信开发者工具
2. 点击"导入项目"
3. 选择`miniprogram`目录
4. 输入您的小程序AppID
5. 点击"导入"

### 4. 测试和发布

1. 在微信开发者工具中测试小程序功能
2. 点击"上传"按钮，将小程序上传到微信公众平台
3. 在微信公众平台中提交审核
4. 审核通过后，发布小程序

## 功能测试

### 1. 留言板功能测试

1. 访问网站首页
2. 滚动到留言板区域
3. 填写留言表单（姓名、邮箱、留言内容）
4. 点击"提交留言"
5. 检查是否显示"留言提交成功"提示
6. 查看留言列表，确认留言是否显示

### 2. 睡眠数据记录功能测试

1. 访问网站首页
2. 滚动到睡眠记录区域
3. 调整睡眠时长和质量滑块
4. 填写备注
5. 点击"记录睡眠"
6. 检查是否显示"睡眠数据记录成功"提示
7. 查看睡眠统计，确认数据是否更新

## 常见问题及解决方案

### 1. CloudBase连接失败

**问题**：服务器启动时显示"CloudBase SDK 未安装或连接失败，使用本地模式"

**解决方案**：
- 检查`.env`文件中的`CLOUDBASE_ENV_ID`和`CLOUDBASE_REGION`是否正确
- 确保您的CloudBase环境已经创建完成
- 检查网络连接，确保服务器能够访问CloudBase服务

### 2. 留言无法提交

**问题**：填写留言表单并提交后，显示"网络连接失败"提示

**解决方案**：
- 检查浏览器控制台是否有错误信息
- 确保服务器正在运行
- 检查API请求URL是否正确
- 检查CloudBase数据库权限是否配置正确

### 3. 睡眠数据无法记录

**问题**：记录睡眠数据后，显示"请求处理失败，请稍后重试"

**解决方案**：
- 检查浏览器控制台是否有错误信息
- 确保服务器正在运行
- 检查CloudBase数据库中的`sleep_data`集合是否已创建
- 检查数据库权限是否配置正确

### 4. 部署到Vercel后无法连接CloudBase

**问题**：部署到Vercel后，留言和睡眠数据无法保存到CloudBase

**解决方案**：
- 检查Vercel项目的环境变量是否正确配置
- 确保`CLOUDBASE_ENV_ID`和`CLOUDBASE_REGION`环境变量已添加到Vercel项目中
- 检查CloudBase环境的访问权限，确保Vercel服务器能够访问

## 技术支持

如果您在部署过程中遇到任何问题，可以通过以下方式获取帮助：

- 查看项目代码中的注释和文档
- 检查浏览器控制台和服务器日志
- 访问[CloudBase官方文档](https://cloud.tencent.com/document/product/876)
- 访问[Vercel官方文档](https://vercel.com/docs)

## 版本历史

- v1.0.0：初始版本，包含基本的个人作品集功能
- v1.1.0：添加留言板和睡眠数据记录功能
- v1.2.0：优化响应式设计，支持手机和电脑
- v1.3.0：修复API连接问题，完善错误处理机制
- v1.4.0：优化部署配置，支持Vercel和微信小程序部署

## 许可证

本项目采用MIT许可证，详情请查看LICENSE文件。
