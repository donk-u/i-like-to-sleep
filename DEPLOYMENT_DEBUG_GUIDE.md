# 部署调试文档：部署到Vercel并使用所有功能

## 1. 项目状态概述

项目已从后端驱动重构为纯前端项目，通过CloudBase SDK直接连接到CloudBase服务，实现所有功能。

- ✅ 已删除所有后端文件和依赖
- ✅ 前端直接连接CloudBase
- ✅ 支持微信和手机号登录
- ✅ 支持留言板带图片上传和回复
- ✅ 支持AI睡眠质量分析

## 2. Vercel部署指南

### 2.1 修改Vercel配置

当前`vercel.json`配置包含了已删除的`server.js`文件引用，需要修改为纯静态网站配置：

```json
{
  "version": 2,
  "builds": [
    {
      "src": "**/*",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
```

### 2.2 部署步骤

1. **安装Vercel CLI**（如果未安装）：
   ```bash
   npm install -g vercel
   ```

2. **登录Vercel**：
   ```bash
   vercel login
   ```

3. **部署项目**：
   ```bash
   vercel
   ```

4. **按照提示配置**：
   - 选择团队或个人账户
   - 确认项目根目录
   - 确认框架预设为"Other"
   - 确认构建命令：留空
   - 确认输出目录：留空
   - 确认是否覆盖现有项目：根据需要选择

5. **访问部署的网站**：
   部署完成后，Vercel会提供一个URL，如`https://your-project.vercel.app`

### 2.3 生产环境部署

```bash
vercel --prod
```

## 3. CloudBase配置检查

### 3.1 确认CloudBase配置

检查`index.html`中的CloudBase配置是否正确：

```html
<script>
    // CloudBase初始化配置
    window.cloudbaseConfig = {
        env: 'cloud1-3gc4eoi9a5139d21',
        region: 'ap-shanghai'
    };
    // ...
</script>
```

### 3.2 确认CloudBase服务已启用

在CloudBase控制台确认：
- ✅ 数据库已启用
- ✅ 存储已启用
- ✅ 认证已启用（微信登录、手机号登录）

### 3.3 确认数据库集合

确保已创建以下集合：
- `guestbook`：用于存储留言
- `sleep_data`：用于存储睡眠记录

## 4. 功能测试指南

### 4.1 登录功能测试

1. 点击页面右上角的"登录"按钮
2. 测试微信登录：
   - 点击"微信登录"标签页
   - 点击"微信登录"按钮
   - 确认微信授权后可以正常登录
3. 测试手机号登录：
   - 点击"手机登录"标签页
   - 输入手机号，点击"发送验证码"
   - 输入验证码，点击"登录"
   - 确认可以正常登录

### 4.2 留言板功能测试

1. 确保已登录
2. 填写留言内容，点击"发表留言"
3. 确认留言成功显示
4. 测试图片上传：
   - 确保留言板支持图片上传（需要在前端代码中添加图片选择功能）
5. 测试回复功能：
   - 点击留言下方的"回复"按钮
   - 输入回复内容，点击"发送回复"
   - 确认回复成功显示

### 4.3 AI睡眠分析测试

1. 滚动到"睡眠记录"区域
2. 调整睡眠时长和质量滑块
3. 点击"AI智能分析"按钮
4. 确认显示AI分析结果和建议

## 5. 常见问题排查

### 5.1 CloudBase连接失败

**症状**：浏览器控制台显示"CloudBase SDK加载失败"或"初始化失败"

**解决方案**：
1. 检查`index.html`中的CloudBase配置是否正确
2. 确认CloudBase环境ID和区域正确
3. 检查网络连接，确认可以访问CloudBase服务
4. 确认CloudBase SDK版本兼容

### 5.2 登录失败

**症状**：微信或手机号登录失败

**解决方案**：
1. 确认CloudBase认证服务已启用
2. 确认微信登录和手机号登录已在CloudBase控制台配置
3. 检查浏览器控制台错误信息
4. 确认前端代码中登录函数调用正确

### 5.3 留言板功能异常

**症状**：无法发表留言或回复，图片上传失败

**解决方案**：
1. 确认已登录
2. 检查CloudBase数据库权限配置
3. 检查CloudBase存储权限配置
4. 检查前端代码中API调用是否正确

### 5.4 AI分析不显示

**症状**：点击"AI智能分析"按钮后无响应

**解决方案**：
1. 检查前端代码中`AI.aiSleepAnalysis`函数实现
2. 检查浏览器控制台错误信息
3. 确认睡眠数据格式正确

## 6. 调试工具使用

### 6.1 浏览器控制台

使用浏览器开发者工具检查：
- 网络请求
- JavaScript错误
- CloudBase连接状态
- API调用结果

### 6.2 功能测试脚本

项目包含`test.js`测试脚本，可以在浏览器控制台运行：

```javascript
// 在浏览器控制台运行
window.testProject();
```

测试脚本会检查：
- CloudBase连接
- API实例
- 交互功能
- HTML元素存在性

## 7. 代码调试技巧

### 7.1 检查API调用

重点检查以下文件中的API调用：
- `script.js`：登录表单处理
- `interactions.js`：留言板和AI分析
- `api.js`：CloudBase API封装

### 7.2 检查CloudBase SDK版本

确认使用的CloudBase SDK版本与CloudBase控制台兼容：

```html
<script src="https://cdn.jsdelivr.net/npm/@cloudbase/js-sdk@2.11.2/dist/index.umd.min.js"></script>
```

### 7.3 检查数据库权限

确保CloudBase数据库权限配置允许前端直接访问：

```json
{
  "read": true,
  "write": true
}
```

## 8. 优化建议

1. **添加错误处理**：在关键API调用处添加更详细的错误处理
2. **添加加载状态**：在异步操作期间显示加载指示器
3. **添加表单验证**：增强表单验证，提高用户体验
4. **优化图片上传**：添加图片压缩和预览功能
5. **添加日志记录**：添加前端日志，便于调试

## 9. 联系方式

如遇到无法解决的问题，请联系：
- 邮箱：1762079094@qq.com
- GitHub：[项目仓库](https://github.com/sleeper)

## 10. 更新日志

- 2025-12-14：创建部署调试文档
- 2025-12-14：添加Vercel配置修改指南
- 2025-12-14：添加功能测试和常见问题排查

---

**文档更新说明**：此文档将根据项目更新持续维护，确保部署和使用指南的准确性。